# 26. 零钱兑换（Coin Change）· 中等

给定不同面额的硬币 `coins` 和一个总金额 `amount`，计算可以凑成总金额所需的最少硬币个数。如果没有任何一种组合能凑出该金额，返回 -1。每种硬币数量无限。

## 示例

```
输入：coins = [1,2,5], amount = 11
输出：3
解释：11 = 5 + 5 + 1

输入：coins = [2], amount = 3
输出：-1
```

## 考点

- 动态规划（完全背包），O(amount × n)。
