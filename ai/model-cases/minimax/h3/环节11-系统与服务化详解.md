# 环节 11 · 系统与服务化（IR + Base + Regen）

> 全链路第十一站。对照 Transformer [环节 11](../../../../foundation/transformer/环节11-服务化与推理引擎详解.md)：LLM 服务化是连续批处理、Paged KV、前缀缓存。H3 服务化是 **异步视频任务 + 双 DiT 分区 + 可选托管 IR/Regen**。操作清单见 [本地部署与 API](../MiniMax-H3本地部署与API接入.md)。
> 所属总揽：[环节00](./环节00-总揽与环节导航.md)。上一站 [环节10](./环节10-Flow采样与推理详解.md)。本站收口。
> 配套 Notebook：[环节11-系统与服务化演示.ipynb](./环节11-系统与服务化演示.ipynb)——三模块责任表；768 vs 2K 你在比什么。

---

## 1. 完整系统仍是三块

```
用户请求
  → H3-Context-IR     托管     环节02
  → H3-Base 768p      开源     环节03–10
  → H3-Regenerate-2K  托管     本环节
```

Regenerator：**不是**独立超分网。让基模以 in-context 方式重画自己的 768p，并再次看见原始多模态上下文。官方理由：

1. 最大复用已经会的生成能力；
2. 小字/品牌细节传统 SR 只能猜，这里可以「再看见原图」。

这也是任务泛化的例子：超分不必另养专家。模块未开源。验证走 Regeneration API 或一键 create。

**选型坑**：开源 768p 对 App 2K，比的是系统。

## 2. 四套推理栈（不要混文件布局）

| 栈 | 适合 |
|----|------|
| ComfyUI ≥ 0.30 | 工作站、LoRA |
| SGLang Diffusion | 生产 HTTP、多卡菜谱 |
| vLLM-Omni | OpenAI 风格视频端点 |
| diffusers Modular | Python、offload、workflow=`fl2va`/`ref2va` |

官方数字：单 Transformer 分区 bf16 ≈ **61.7 GB**；Qwen 条件器 ≈ **62.1 GB**。两份 DiT 默认都 load 时显存再翻。`--task-type` / `--model-variant` 只留一份。

## 3. 和 LLM 网关的差别

| LLM | H3 |
|-----|-----|
| 同步流式 token | 异步任务；分钟级很正常 |
| `/v1/chat/completions` | `/v1/videos` 或 `video-generation-v2-*` |
| 前缀缓存命中 | 参考 latent 缓存、AdaLN 表 |
| 连续 batch 不同长度 decode | 不同时长 = 不同 \(n\)，要负载均衡（训练侧官方已强调方差 ×3） |

完整 2K 本地复现：SGLang Base + 官方 IR + Regen；768p 可纯本地。

## 4. LoRA 挂在哪

社区人像 LoRA 打共享 `attn.qkv_proj`，一份文件挂 T2V/I2V/R2V，**不替代** IR/Regen，也不合并 FL2VA/Ref2VA 两份基座。公式仍是 \(W+\gamma BA\)，见 [PEFT 环节04](../../../../foundation/peft-lora/环节04-低秩适配公式详解.md)。

## 5. 工程要点

1. 审核在 IR 段也会做；本地 Base 的安全责任在你。
2. 许可证排除美/欧/英/韩领土、蒸馏禁令、营收门槛、UI 展示「MiniMax H3」——上线前读原文。
3. 路线图：H 系下一版计划吸收 M 系理解；**不是**现网已经合成一个模型。

## 6. 面试追问

- **2K 是超分吗？** 官方定义为基模 in-context 重生成。
- **开源缺什么导致不像 App？** 主要缺环节 02 和本站 Regen，其次缺官方 IR 补全的 caption 质量。

## 7. 系列收口

回到 [环节 00](./环节00-总揽与环节导航.md) 那句话：H3 是 packed 联合去噪机。Transformer 系列教你「接龙机」的零件；本系列教你同一套 Attn/FFN/RMS/RoPE 如何改行去运噪声。两条生命周期不要混着调参。
