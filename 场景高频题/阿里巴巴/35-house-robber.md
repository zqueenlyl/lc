# 35. 打家劫舍（House Robber）· 中等

给定一个非负整数数组 `nums` 表示每个房屋存放的金额，相邻房屋装有相互连通的警报系统，不能连续偷窃相邻房屋。求一夜之内能偷到的最大金额。

## 示例

```
输入：nums = [1,2,3,1]
输出：4
解释：偷 1 号（1）和 3 号（3），共 4。
```

## 考点

- 动态规划（状态转移 dp[i] = max(dp[i-1], dp[i-2]+nums[i])），O(n)。
