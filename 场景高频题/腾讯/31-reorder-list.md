# 31. 重排链表（Reorder List）· 中等

给定单链表 `L: L0 → L1 → … → Ln-1 → Ln`，将其重新排列为 `L0 → Ln → L1 → Ln-1 → L2 → Ln-2 → …`。不能只改变节点值，必须实际交换节点。

## 示例

```
输入：head = [1,2,3,4]
输出：[1,4,2,3]
```

## 考点

- 找中点 + 反转后半段 + 交叉合并，O(n)。
