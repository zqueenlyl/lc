# 30. 字符串转换整数 (atoi)（String to Integer）· 中等

实现一个 `atoi` 函数，将字符串转换为 32 位有符号整数。规则：忽略前导空格，识别可选正负号，读取连续数字字符；若溢出则截断到 `INT_MAX`(2^31-1) 或 `INT_MIN`(-2^31)；无效开头返回 0。

## 示例

```
输入：s = "42"
输出：42

输入：s = "   -42"
输出：-42

输入：s = "4193 with words"
输出：4193
```

## 考点

- 逐字符解析 + 溢出判断，O(n)。
