# 环节 01 · Tokenizer 分词（文本 ↔ 编号）

> 全链路第一站：把"人话"翻译成"编号"。
> 所属总揽：[Transformer全链路总揽与环节拆解.md](./Transformer全链路总揽与环节拆解.md) 环节 01。
> 相邻环节：入口站（无上游）→ 下一站 [环节 02 · Embedding 查表](./环节02-Embedding查表详解.md)（编号 → 向量）。
> 适合人群：后端/资深读者。工程上 token 数 = 账单、= 上下文占用、= 吞吐指标，这个环节直接决定"钱花在哪"。

---

## 1. 干什么

把一段文本切成**有意义的原子单位（token）**并映射成整数 id。

一次切分决定三件事：

1. 模型"字库"有多大（词表大小 |V|，直接决定 Embedding 层参数量）；
2. 文本被压缩得多紧（同样的句子，token 数越少，训练/推理/账单越省）；
3. 生僻字、新词、多语言怎么兜底（切不出来的片段如何表示）。

**最小直觉**：模型内部没有任何文字，只有编号。以"我爱猫"为例：

```
"我爱猫" → 切块 [我] [爱] [猫] → 查编号本 → [2] [7] [35]
```

"我爱猫"变成一串编号后，才能进入 [环节 02 Embedding 查表](./环节02-Embedding查表详解.md)。

> **产物口径：这个环节到底"输出"了什么？——被问到时先分清问的是哪一层**
>
> **视角 A · 问"环节自身流程的产物" → 输出的是一个完整 tokenizer 组件，词表只是核心数据。**
> 词表（token ↔ id 双向映射）只是组件四件套之一，**光有词表跑不起来**：
> - **词表 vocab**：token ↔ id 映射——最常被称作"词汇表"的部分；
> - **merges / rank**：BPE 合并规则的先后顺序，贪心最长匹配怎么切全看它；
> - **预切分正则 + 字节兜底**：先怎么切块、词表外内容如何退到 UTF-8 字节；
> - **特殊 token / 格式 token**：`<|endoftext|>`、`<|im_start|>` 等边界标记。
>
> 在 HF 上对应 `tokenizer_config.json` + `tokenizer.json` + `special_tokens_map.json` 一组文件。
>
> **视角 B · 问"交给下游模型的交付物" → 不是词表，而是 id 序列。**
> 预训练时：语料 → encode → **id 序列**喂给模型（模型只见过 id，从未见过文本）；词表被加载成查表字典后常驻内存，并不流向下一环节。推理时反向：模型生成 id 序列 → decode → 文本。
>
> **一句话收口**：环节01 的产物是"文本 ⇄ id"的编码能力，词表是这项能力的参数/状态——所以强调词表冻结随模型一起发布（§7），本质是锁死 id 语义不变。

---

## 2. 基本单元：三种粒度怎么选

| 粒度 | 怎么切 | 优点 | 缺点 |
|---|---|---|---|
| **字符级（character-level）** | 按字符切 | 词表极小、无死角 | 英文一个词被拆成 5~8 个字符 → 序列变长、算力浪费 |
| **词级（word-level）** | 按空格/分隔符切词 | 词数少、语义完整 | 词表爆炸（几十万起）；**生词/OOV（out-of-vocabulary，词表外词）无法处理** |
| **子词级（subword，主流）** | 常用词整词，生词拆成更小片段 | 兼顾"词表小 + 无死角" | 需要训练算法（见下） |

子词拆分的直观例子：`unhappiness → un + happiness`（或 `un + happi + ness`）。常用词保整词，生词/变形词能拆回片段，任意新词都能被兜住。

**中文注意**：中文无空格，模型见的是"字/词片段"的编号——**不要用 jieba 这类语言学"中文分词"先切词再喂模型**，交给 SentencePiece 或 HF tokenizers（§4）自动切即可；主流模型对中文多用字节级 BPE，各家实际方案见 §5.3。

---

## 3. 主流子词算法

三种算法 + 工程封装：

| 算法 | 核心做法 | 优点 | 缺点 | 代表 |
|---|---|---|---|---|
| **BPE**（Byte Pair Encoding） | 反复合并语料中出现频率最高的相邻"符号对"，直到词表达到目标大小 | 简单、无损、实现高效 | 合并是贪心的，可能产出语义碎片（`ing` 被拆散） | GPT 系 |
| **WordPiece** | 类似 BPE，但合并标准是"让整体似然提升最大"（而非纯频率） | 语言学上更优的切分 | 实现更复杂、训练更贵 | BERT |
| **Unigram** | 反向操作：从超大字表出发，按 loss 最小逐步删除最不重要的子词 | 每个位置可产出多种候选 → 采样/正则化友好 | 训练较重 | T5（SentencePiece 的 unigram 模式） |

### 3.1 BPE 的通用流程（了解即可，细节由库实现）

```
1. 把语料按词切分，并拆成"字符序列 + 词尾标记"，如 happy→ h a p p y </w>
2. 统计所有相邻符号对的共现频率
3. 每一轮：取频率最高的一对 → 合并成一个新符号，写进词表
4. 重复 3 直到达到目标词表大小（或合并收益低于阈值）
编码时：查表把文本贪心切成长词优先；出现未登录组合则一路回退到字节/字符兜底
```

要点：BPE 输出的是**一套合并规则（merge rules）**（等价于最终词表），可无损还原；合并顺序靠语料（corpus）统计，"哪个片段更常见就先固化成词"。

### 3.2 中文场景的 BPE：手推示例（无空格、无预分词）

英文 BPE 的起步是"空格切好的词 + 词尾标记"（`happy → h a p p y </w>`）；**中文没有词边界**，所以起步符号直接是语料里出现的"字"（字符级）或 UTF-8 字节（字节级，主流模型路线）。下面用字符级演示合并机制；字节级只是把"字"再下探一层到 256 个字节，合并逻辑完全相同。

**语料**（整段当作字流，不做任何分词）：

```
学习人工智能 学习人工智能 未来 未来 人工智能
→ 学 习 人 工 智 能 学 习 人 工 智 能 未 来 未 来 人 工 智 能
```

| 轮次 | 本轮最高频相邻对 | 频次 | 合并产物 |
|---|---|---|---|
| 1 | `人+工` | 3 | `人工` |
| 2 | `人工+智` | 3 | `人工智`（暂时是"碎片"，贪心合并不保证每步漂亮） |
| 3 | `人工智+能` | 3 | `人工智能`（高频串被多轮拼出） |
| 4 | `学+习` | 2 | `学习` |
| 5 | `未+来` | 2 | `未来` |
| … | 继续直到词表预算耗尽 | | |

合并规则：每轮重数相邻对频次，取最高者合并；同频按首次出现顺序。**句子/词边界（word boundary）不跨边界统计**（真实实现以空格或专用边界符号隔离，SentencePiece 用 `▁`）。

> **补注 · "整个语料库"：合并是全库全局统计，且只发生在离线训练阶段**
> - 上表频次（3、2…）只是把语料缩到一句话方便手算；真实训练在预训练语料的**代表性抽样**（数百 GB，按语言/领域配比）上进行，每轮对**全库**统计相邻对频次取全局最高者合并，频次量级是千万而非个位。
> - 工程实现不会每轮全库重扫（128K 词表 ≈ 上万轮 × 全扫不现实）：只对语料完整统计一次相邻对，合并后仅**增量更新受影响的局部邻接对**（替换点前后的 pair 变了），再用**优先队列**维护"当前全局最高频对"；部分实现还支持一轮并行合并多个互不相邻的对。
> - **推理/编码阶段语料不再参与**——词表已冻结，纯查表做贪心最长匹配（见下文编码示例），单条输入再长也改不动词表。
> - 推论：词表反映**训练语料的全局偏好**——语料单一领域（如全是代码）会把词表带偏，这就是 tokenizer 训练语料要跨语言/领域配比的原因。

**编码（推理侧，贪心最长匹配）**：

```
"人工智能未来已来"   → [人工智能][未来][已][来]     # 整词优先
"未来已来" 的 "已来" → 词表无此条目 → 拆单字 [已][来]  # 语料内单字兜底
"未来人工智能"       → [未来][人工智能]               # 已知词拼出新句子，照常切分
"人工智障"(含语料外字"障") → 字符级 BPE 卡住 → 需字节级兜底（见下）
```

关键结论：
- 高频组合通过**多轮合并**逐渐固化成"词"——词表是语料统计的产物，不是人工词典，这也是中文不需要"分词器"的原因（详见 §5.2 对照）；
- 训练产物里出现 `人工智` 这类"中间碎片"完全正常——**BPE 只要求无损可还原，不求词条语言学优雅**；
- 语料内的任意字组合（哪怕不在词表里）都能"整词优先 / 逐字兜底"地切出来；**语料外的生僻字、emoji 由字节级 BPE 从 256 字节兜底**——主流模型全部是字节级，所以"任意输入都能编码"不是字符级 BPE 的功劳，而是字节级兜底（对应 §5.3）。

> 字节级说明：真实主流模型（GPT / Qwen / DeepSeek）用字节级 BPE——汉字先拆成 3 个 UTF-8 字节再合并；高频汉字会迅速合并回"1 字 ≈ 1 token"，额外多的是"任意 Unicode 都能从 256 字节出发重建"这层兜底（详见 §5.3）。

### 3.2b tokenizer 训练语料的配比：方法论与工程

上面频次是"一句话语料"的演示；真实里**词表长什么样，完全取决于喂给 BPE 的语料配比**——谁重复多谁先进词表。做法三步 + 验证闭环：

- **① 先定目标再凑语料**（不是"有什么用什么"）：列出目标语言与期望权重（如中文优化：中文 40% / 英文 40% / 代码 15% / 其它 5%）；对低资源语言设**最低保障**——常用汉字 top-N、常用词必须出现足够多次，否则合并永远轮不到它们。
- **② 清洗去重必须在配比之前**：不去重会把重复文本的频次刷爆、污染词表并虚增语料规模；手段 = MinHash/SimHash 近重复去重 + 精确子串去重 + 质量过滤（quality filtering）。
- **③ 按目标比例重采样（resampling）而非拼接**：单位是 **token 级而非文档/字符级**（不同语言压缩率不同，同量 token 的中英文内容量不同）；低资源语言过采样（oversampling）、高资源语言欠采样（undersampling），混合成一条语料流去训 BPE。

**验证闭环**：训完在固定评测集上按语言量 `bytes/token` 压缩率（compression ratio）、生僻字回退率（fallback rate）、目标语言 top-N 覆盖——不达标就调比例再来一轮（`bytes/token` 的锚点与自查口径见小节尾注）。

**红线**：tokenizer 的语言配比要与**预训练语料配比同源对齐**——词表给了中文充足空间但预训练只占 2%，中文照样弱；反之词表没中文 token、预训练却占 40%，中文就被拆成碎片（预训练 data mixing 的方法论见环节09）。

> **尾注 · bytes/token 锚点与"token 是成本等价物"**：`bytes/token` = 原文 UTF-8 字节数 ÷ token 数，指"每个 token 平均包多少原文"——只在**同语言内**衡量词表好坏、估算语料规模；不同语言拿数值直接比，没有"谁更好"的含义。
> - **英文锚点 ≈ 4.1**：空格可预切词，平均"词+尾空格"约 5.5 字节，子词合并后 1 token ≈ 0.7~0.8 词 → 5.5 × 0.75 ≈ 4.1；
> - **中文锚点 = 3（单字基线）**：高频汉字必然被合并成"1 字 = 1 token"，汉字 UTF-8 恰为 3 字节；双字词合成 1 token → 升到 6，生僻字被拆成字节 token → 掉回 1~3。故中文实测在 3 附近浮动——**词表做中文优化的本质，就是把常用双字词塞进词表、把该值推到 3 以上**；低于 3 说明该词表对中文不友好（老 cl100k 即如此，1 字 ≈ 1.4~1.6 token，"中文 token 贵"的印象正来自这个代际）。
> - **同 1M token 对应的原文量不同**：英文 ≈ 75 万词 ≈ 4.1MB；中文 ≈ 60~100 万字 ≈ 2~3MB（随词表 0.6~1.5 token/字浮动）。一句话：**token 是成本等价物，不是内容等价物**——1M token 算力成本相同，覆盖的原文字节/语义量却因语言而异；配比按 token 级给是"成本公平"，不是"内容公平"。
> - 预训练配比里的两个概念（详见环节09）：**token 级配比** = 模型成本 ≈ 6ND 以 token 计，故按最终 token 份额分配、让"名义比例 = 实际训练份额"；**Pareto 最优** = 预算固定时"英/中/代码/数学"多目标不可兼得，各配比落在 Pareto 前沿、无唯一最优，选点取决于主打能力权重。

### 3.3 WordPiece / Unigram 的本质差异

- **BPE 比"频率"**：谁出现得多谁先被合并（纯统计、贪心）。
- **WordPiece 比"似然增益（likelihood gain）"**：合并后语言模型困惑度（perplexity，PPL）下降最多者优先（统计学 + 一点语言模型视角）。
- **Unigram 反过来"删"**：先给超大字表，用 EM 迭代（Expectation–Maximization，期望最大化）估计每个子词在语料里的概率，逐步删掉让总 log-likelihood 损失最小的子词；因为建模了"每个位置有哪些候选子词"，天然支持带正则/采样的切分。

---

## 4. 工程封装与字节级 BPE

纯算法之外，落地几乎都走现成库：

- **SentencePiece**：直接把文本当字节/字符流处理，**不依赖空格预分词**——对中日韩这类无空格语言是刚需（早期 LLaMA / ChatGLM 等用它）。注意它只是"库 + 模式"，主流 LLM 近年更偏好字节级 BPE（DeepSeek/Qwen 等实际不是 SentencePiece，见 §5.3）。
- **TikToken**：OpenAI 系的 tokenizer 实现（GPT 系列词表），以效率著称。
- **HuggingFace tokenizers**：生态事实标准，Rust 内核、训练/保存/加载一键完成。
- **字节级 BPE**（byte-level，GPT-2 首创）：把 UTF-8 字节当"最小符号"再跑 BPE，**任意 Unicode、emoji、乱码都能兜住**，不会被 OOV 卡死——这是 GPT 系"什么都能输入"的底层保证。

> 面试点：问"中文/生僻字怎么不崩"，答"字节级 BPE / SentencePiece"；问"为什么能处理任何语言的任何文本"，根在字节级兜底。

---

## 5. 中文生态：语言学分词 vs LLM Tokenizer（主流模型实测）

传统中文信息处理里有一门独立的"中文分词"，它和 LLM 的 tokenize 是**两个世界**——先分清，再看各家实际用什么。

### 5.1 两个世界的本质差异

| | 语言学分词（word segmentation，老 NLP） | LLM tokenizer |
|---|---|---|
| 目标 | 还原"词"的边界，服务词性（part-of-speech，POS）/句法（syntax）/检索/知识图谱（knowledge graph） | 压缩序列 + 消灭 OOV |
| 中文做法 | 显式切"词"（jieba / HanLP / CRF…） | 字节/子词级自动合并，无显式切词 |
| 词典/词表 | 靠人工维护的词典 | 词表从语料训练自动生成 |
| 歧义（ambiguity）处理 | 分词器负责消歧 | **不消歧，交给模型上下文（context）** |
| 现役场景 | 检索、知识图谱、传统流水线 | 一切 LLM 应用 |

> 工程红线：LLM 应用侧（RAG 切片、长度校验、账单估算）**一律用模型自己的 tokenizer，别拿 jieba 先分词**——两者对同一句子的切法与数量完全不同，混用会导致计费和截断全部失真。

### 5.2 传统中文分词方法谱系（了解，老场景仍会用到）

四流派演进：词典（lexicon）机械匹配（FMM/BMM/双向）→ 词典 + DAG（有向无环图，directed acyclic graph）动态规划（dynamic programming）求最优路径（jieba 思路）→ 序列标注（sequence labeling；HMM 隐马尔可夫模型 → CRF 条件随机场 → BiLSTM/BERT，打 B/M/E/S 标签，质量最高）→ 统计新词发现（PMI + 左右信息熵，专治词典漏词）。

工具生态：**jieba**（最普及）、pkuseg（北大，跨领域/细粒度）、HanLP（多任务一体化）、LTP（哈工大，词性+依存同出）、THULAC（清华）、SnowNLP（轻量脚本）。

共同难点：交集/组合歧义（`研究生命科学` 可切"研究/生命/科学"）、**未登录词**（OOV，回到 §2）、切分粒度（granularity）本身没有标准答案（标注语料彼此都对不齐）。

### 5.3 主流 LLM 实际用什么（速查表）

| 模型 | 算法 | 词表 | 实现 |
|---|---|---|---|
| GPT-4o | 字节级 BPE | ~200K（o200k_base） | tiktoken |
| Llama 3.x | 字节级 BPE | 128K | tiktoken 风格正则预切分 |
| Qwen2.5 / Qwen3 | 字节级 BPE | ~151K~152K | tiktoken 风格 |
| **DeepSeek V3 / R1** | **字节级 BPE** | **约 128K（config=129,280）** | **HF tokenizers 自研自训** |
| Baichuan（早期） | BPE | ~64K | SentencePiece |
| 腾讯混元家族（历史） | 字节级 BPE | ~128K | tiktoken 风格 + 中文扩展（二手转述） |
| **混元 Hy4 preview** | 官方未披露 | 未披露 | 待技术报告 |

共性规律：**中文优化 = 大词表 + 字节级兜底 + 从头训 merge 规则**；没有任何主流模型在内部接 jieba/CRF 那套语言学分词器。

**DeepSeek 细节（可当标准答案讲）**：
- 字节级 BPE，用 HuggingFace tokenizers 从中英等多语言预训练语料**从头训练**，非复用 GPT/Qwen 词表；
- 词表约 128K（HF `config.json` 里 `vocab_size=129280` = 128K + 特殊 token）；
- 中文不做分词：按 UTF-8 字节流自动合并出片段，任意生僻字/emoji 都有字节兜底（无 OOV）；
- **代际之间会重训 tokenizer**（V2 与 V3 词表不同）→ 升级模型必须整体迁移，别只换权重。

**混元 Hy4 preview（2026-08-28 发布并开源：770B 总参 / 49B 激活 / 1M 上下文）**：
- 截至本笔记记录（2026-09），官方发布稿**未披露 tokenizer 方案**，技术报告待出；
- 旁证：混元历史家族（Hunyuan-Large 系）用 ~128K 字节级 BPE + 中文词表扩展，Hy4 大概率同族延续——**属推断，非官方口径**；
- 网上流传"Hy4 tokenizer 完全兼容 jieba+pkuseg 生态"是营销号假消息，勿信；
- 注意 1M 上下文与分词器无关，靠的是稀疏注意力/位置编码/长文课程训练，别把两者混为一谈。

### 5.3b 开源库一览 + 工程选型

| 库 | 出品 | 能训/能编 | 算法 | 一句话定位 | 典型用户 |
|---|---|---|---|---|---|
| **HF `tokenizers`** | HuggingFace | 能训能编 | BPE / WordPiece / Unigram | 现代默认（Rust 内核，快），2023 后新开源模型几乎全用它 | DeepSeek、Qwen、绝大多数 HF 模型 |
| **`sentencepiece`** | Google | 能训能编 | Unigram / BPE | 无空格语言友好（`▁` 表空格）；旧多语模型重地 | LLaMA 1/2、Baichuan、ChatGLM、T5/XLM-R（Unigram） |
| **`tiktoken`** | OpenAI | **只编不训** | 读 BPE 词表文件 | GPT 系编码与计费换算；"tiktoken 风格"指仿它的正则预切分+字节级 | GPT-4o（o200k_base）/ GPT-4（cl100k_base）/ GPT-3（r50k_base）；LLaMA3、Qwen 为同风格 |
| **`minbpe`** | Karpathy | 教学向能训 | 正则/字节级 BPE | 几百行可读，学原理用 | 学习演示 |
| `transformers.AutoTokenizer` | HuggingFace | 统一加载层 | 底层自动选 | 日常调开源模型的入口，细节不用管 | 所有模型 |

**关键澄清**：`tiktoken` 只管"读词表 + 编码"，**不负责训练**——`o200k_base`/`cl100k_base`/`r50k_base` 是 OpenAI 预先练好的词表文件；真正"从语料训词表"在 HF `tokenizers` 或 `sentencepiece` 里做。

**工程选型一句话**：加载开源模型 → `AutoTokenizer.from_pretrained(...)`；自训词表 → HF `tokenizers`（推荐）或 `sentencepiece`；对齐 GPT 词表做计费/前后处理 → `tiktoken`；学原理 → `minbpe`。

**生态趋势**：2023 年后新模型几乎全部切到 HF `tokenizers` 或 tiktoken 风格的字节级 BPE；SentencePiece 主要留守老一代模型与多语学术模型（Unigram 场景仍不可替代）。

### 5.4 如何自行验证一个开源模型的分词方案

拿到开源权重目录后 4 步，不依赖官方论文：

1. `tokenizer_config.json` → `tokenizer_class`（如 `PreTrainedTokenizerFast`）与实现线索；
2. `tokenizer.json` → `model.type` 是 `BPE` / `WordPiece` / `Unigram` 之一；再看 `pre_tokenizer` 的结构判断是否"正则预切分 + 字节级"；
3. `config.json` → `vocab_size`：词表规模（对照是否与声称的 128K/200K 一致）；
4. 用该 tokenizer 试编码一段含**生僻字 / emoji / 换行 / 数字 / 中英混排**的文本：观察是否回退字节 token、每字折合几个 token——压缩率和"语言友好度"一目了然。

### 5.5 面试快答

- 中文/生僻字为什么不会崩？→ 字节级 BPE / 字节兜底；无空格语言靠 SentencePiece 或字符流式处理。
- LLM 为什么不用 jieba 分词？→ 切分粒度由训练决定，歧义交给上下文；显式分词只会引入错切与 OOV，还抬高集成成本。
- 怎么快速判断陌生模型用哪种分词？→ 看 `tokenizer.json` 的 `model.type` + `vocab_size` + 试编码特殊文本（§5.4）。

---

## 6. 关键设计取舍

| 决策 | 取舍 |
|---|---|
| **词表大小 |V|** | 太小 → 长句被拆碎、序列变长；太大 → Embedding 层参数量爆炸（|V| × d，130k × 4096 ≈ 5.3 亿参数）。现代模型通常 50k~200k。 |
| **特殊 token** | `<|endoftext|>`、`<|im_start|>`（Chat 格式）、`<pad>`、保留位等——**格式的边界也是词表的一部分**，决定了指令模板能否正确工作 |
| **数字/空白处理** | 要不要把数字合并、空格是否显式建模（SentencePiece 用 `▁` 表示空格），影响代码/表格类内容的表现 |

**为什么词表大小影响上下文与账单**：同样的语义，token 化得越碎序列越长——注意力的 O(n²)、KV Cache 显存、每 token 计费全部随之上涨。这也是中文场景常被吐槽"token 单价隐性偏高"的原因（中文字符在部分词表里 1 字 ≈ 1~2 token）。

### 6.1 词表大小专题：多少合适、按什么定、多大多小各影响什么

**一句话结论**：词表大小没有全局最优解，它是**「压缩收益」vs「参数与长尾成本」**的三方权衡，最优值取决于**语料构成 + 部署预算**——所以每家定得不一样（回看 §5.3：GPT-4o 200K、Qwen 151K、LLaMA3/DeepSeek 128K、LLaMA 1/2 只有 32K）。

**① 先看两个方向上的代价（为什么不是越大越好/越小越好）**

| 维度 | 词表偏大（如 200K+）的代价 | 词表偏小（如 10K~30K）的代价 |
|---|---|---|
| 参数/显存 | Embedding 与输出层都是 \|V\|×d：128K×4096≈**5.3 亿参数/张**，且有两张（输入+输出，除非 weight tying）；显存与每步带宽随之上涨 | 参数省、文件小、训练更充分 |
| 压缩率/序列成本 | 压缩率高：同语义 token 更少 → O(n²) 注意力、KV Cache、每 token 计费全降 | 中文几乎 1 字 1 token，序列明显变长 → 全链路成本涨（§6 上表已述） |
| 长尾（long-tail）质量 | 词表名额被大量**低频 token**占满：出现次数太少 → embedding 学不动成为"死 token"，推理时模型也不倾向使用，等于浪费名额 | 生僻内容走字节兜底，反而每个 token 都被训够；代价只是序列变长 |

**② 决定"合适"的三个考量（按思考顺序）**
1. **语料构成/语言配比**（承接 §3.2b）：纯英文/代码语料 32K 已够（英文锚点 ≈4 bytes/token）；中英混合若想中文 1 字 ≤1 token，得把高频双字词/词根收进表 → 通常要 100K+；多语模型（250K 级）每加一门语言都在挤名额，就得牺牲压缩率或接受更大的表。
2. **目标压缩率及格线**：先定"每个 token 要扛多少内容"，如中文锚点 3 bytes/token（§3.2b 尾注）；低于及格线就说明表太小。
3. **部署预算**：上下文上限、Embedding/输出层显存、训练总算力（序列总长直接进 6ND）。词表超过"收益拐点"后，新增的都是低频长尾，几乎不参与日常压缩——纯开销。

**③ 不同数量档位实际长什么样（可当参照系）**

| 档位 | 代表 | 画像 |
|---|---|---|
| ~32K | LLaMA 1/2 | 英文为主够用；中文 1 字 ≈ 1+ token，长文本偏贵；Embedding 小（32K×4096≈1.3 亿），小规模微调友好 |
| ~100K~150K | LLaMA3 128K / DeepSeek 128K / Qwen 151K | **中英平衡线**：高频中文词一条龙入表、代码/JSON 结构词也有名分；Embedding 5~6 亿参数级，主流大厂普遍落点 |
| ~200K | GPT-4o（o200k_base） | 追求更高压缩率与覆盖面（多语/符号广）；Embedding 更大，换来更省序列与计价——OpenAI 的 token 计价与它一致 |

（以上按 d_model=4096 估算参数，d 翻倍则参数翻倍。）

**④ 工程上怎么"定"而不是拍脑袋**
1. 从目标语料抽代表性样本，配比与预训练一致（§3.2b）；
2. 用同一语料训练一串候选词表（如 16K→32K→64K→128K→256K），画 **压缩率-词表大小曲线**，取变平的拐点：拐点后新增名额只是低频长尾，几乎不换压缩收益；
3. 交叉检查长尾占比：统计出现次数极低（如 <10 次）的 token 比例，过高说明表偏大；
4. 对齐实现约束：预留特殊 token 位、尺寸是否需按 64/128 对齐（部分框架并行约束）、Embedding 显存是否吃得住。

**⑤ 面试一句话**：词表大小 = 压缩收益（序列变短省算力）与代价（\|V\|×d 参数、长尾死 token）的平衡点；决定因素是语料构成与部署预算，经验区间英文 32K~64K、中英混合 100K~150K、多语/极致压缩 200K+；落地靠"候选词表 × 压缩率曲线拐点"。

---

## 7. 工程红线（必守）

1. **训练与推理必须用同一份词表 + 同一算法**。token id 对不上 = 输入全变乱码、输出不知所云。升级模型版本时必须校验 tokenizer 是否一致，不一致要重算对齐。
2. **任何文本入口（含 RAG 切片、工具入参）都要先 tokenize 校验长度**，而不是按"字符数"截断——字符数 ≠ token 数，截断点可能把词切坏。
3. **词表冻结**：发布后不要随意增减词表行，除非重训 Embedding；新增 token 属于特殊操作（如给工具调用预留 token）。
4. **特殊 token 对齐指令模板**：Chat 格式的 `<|im_start|>` 等必须与训练时完全一致，否则模型不理解"角色边界"。

---

## 8. 面试高频追问

1. 为什么主流是子词而不是字符/整词？→ 字符序列太长浪费算力；整词表爆炸且有 OOV；子词两全。
2. BPE / WordPiece / Unigram 的核心区别？→ 比频率 / 比似然增益 / 反向删除，方向不同但产出同类词表。
3. SentencePiece 为什么对中文关键？→ 无空格语言无法预分词，它把空格当字符直接建模字符流。
4. 为什么 GPT 能处理任意 Unicode？→ 字节级 BPE，UTF-8 字节兜底，无 OOV。
5. 词表大小怎么定？→ 权衡 Embedding 参数 vs 序列长度 vs 压缩率；经验区间英文 32k~64k、中英混合 100k~150k、多语 200k+，完整框架见 §6.1。
6. 上线换了 tokenizer 会怎样？→ id 语义全错位 = 乱码/胡言，必须走迁移验证。

---

## 9. 相关链接

- 入口总揽：[Transformer全链路总揽与环节拆解.md](./Transformer全链路总揽与环节拆解.md)
- 下一步：[环节 02 · Embedding 查表详解](./环节02-Embedding查表详解.md)（编号 → 向量）
- 知识地图：[知识点.md](../知识点.md)（1.1 Transformer 基础）
- 工程相关（切分/检索侧）：`../../something/RAG工程化.md`
